name: lint
on:
  push:
    branches: [v2]
  pull_request:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        # rules and exemptions live in ruff.toml -- this job just runs `ruff check`
