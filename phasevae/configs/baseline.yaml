# The no-psi §7 recipe: bar-rate + triangle. NO LONGER THE DEFAULT -- see anchor_k.yaml.
# gtzan rule-g F 0.393 measured 2026-08-09 (a banked 2026-08-07 run of the same config
# scored 0.468; nominally identical configs have landed ~0.10 apart, so treat any single
# run of this recipe as 0.39-0.47 rather than a point value).
# Rationale for every key: docs/phasevae_decisions.md; defaults for omitted keys in run.py.
emission: triangle
drift_bound: 0.01
bar_rate: true
pos_weight: 1.0
epochs: 60
beta_warmup: 0        # measured irrelevant without psi (L2 0.496 vs banked 0.505)
