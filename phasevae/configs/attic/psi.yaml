# §9 variant: conditional prior deployed (Sohn-orthodox), phasevae/psi.py.
# Ladder verdict 2026-08-06: rotations=1 reaches parity with baseline (gtzan 0.510);
# the K>1 rotation mixture never learned to carry the anchor -- change only to retry
# the transfer mechanism itself.
variant: psi
rotations: 1
lambda_prior: 1.0
emission: triangle
pos_weight: 1.0
epochs: 60
beta_warmup: 0
