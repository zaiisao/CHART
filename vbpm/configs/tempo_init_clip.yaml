# ARM 2 of the 2026-08-12 tempo-init/clip test: arm 1 PLUS per-tensor clipping. Control = /tmp/thaw10 (F 0.206).
emission: triangle
pos_weight: 1.0
epochs: 60
beta_warmup: 0
excerpt_seconds: 45.0
batch_size: 16
lr: 3.0e-5
kappa_physical: 50.0   # the control's value, NOT the schema default 383
frontend_lr_scale: 1.0
clip_per_group: true
